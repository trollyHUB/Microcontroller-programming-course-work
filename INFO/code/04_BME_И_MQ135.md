# Вариант 04 — BME280 + MQ-135 (рабочий минимум)

**Датчики:** BME280, MQ-135, BH1750, PIR, KY-037  
**Управление:** LCD 1602, RGB LED, Зуммер, 2 кнопки  
**Без:** ZM106-VOC (нет UART), DHT11  

> Это ближайший вариант к проверенному TEST BUILD. Добавлен MQ-135 для аналогового CO₂.  
> Рекомендуется как основной рабочий вариант.

**Библиотеки:** Wire, WiFi, HTTPClient, ArduinoJson, Adafruit_BME280, BH1750, LiquidCrystal_I2C

---

```cpp
/*
 * NEXIS Wellness Station — Вариант 04: BME280 + MQ-135
 * Без ZM106-VOC (UART), без DHT11
 * Arduino IDE — всё в одном файле
 *
 * ← ИЗМЕНИТЬ: Введи свои данные WiFi и IP сервера ↓
 */

#include <Wire.h>
#include <WiFi.h>
#include <HTTPClient.h>
#include <ArduinoJson.h>
#include <Adafruit_Sensor.h>
#include <Adafruit_BME280.h>
#include <BH1750.h>
#include <LiquidCrystal_I2C.h>

// ============================================================
//  НАСТРОЙКИ — ИЗМЕНИТЬ!
// ============================================================
#define WIFI_SSID       "Testardu"       // ← ИЗМЕНИТЬ
#define WIFI_PASSWORD   "12345678"       // ← ИЗМЕНИТЬ
#define SERVER_HOST     "10.78.242.107"  // ← ИЗМЕНИТЬ: IP вашего ПК
#define SERVER_PORT     5000

// ============================================================
//  ПИНЫ
// ============================================================
#define I2C_SDA         21
#define I2C_SCL         22
#define I2C_BME280_ADDR 0x76
#define I2C_BH1750_ADDR 0x23
#define I2C_LCD_ADDR    0x27

#define PIN_MQ135       36   // ADC1 (Input-only, нет конфликта с WiFi)
#define PIN_PIR         27
#define PIN_NOISE       39   // ADC1
#define PIN_LED_R       13
#define PIN_LED_G       12
#define PIN_LED_B       14
#define PIN_BUZZER      25
#define PIN_BTN_MODE    32
#define PIN_BTN_OK      33

// ============================================================
//  ИНТЕРВАЛЫ
// ============================================================
#define SENSOR_INTERVAL_MS   10000
#define DISPLAY_INTERVAL_MS  3000
#define WIFI_RECONNECT_MS    15000

// ============================================================
//  ПОРОГИ АЛЕРТОВ
// ============================================================
#define THRESH_CO2_WARN     800
#define THRESH_CO2_DANGER   1200
#define THRESH_TEMP_WARN    27.0f
#define THRESH_TEMP_DANGER  30.0f
#define THRESH_HUM_WARN     65.0f
#define THRESH_HUM_DANGER   75.0f
#define THRESH_LIGHT_WARN   150
#define THRESH_LIGHT_DANGER 50
#define THRESH_NOISE_WARN   55
#define THRESH_NOISE_DANGER 70
#define NOISE_DB_MIN        30.0f
#define NOISE_DB_MAX        90.0f
#define POMO_WORK_MIN       25
#define POMO_BREAK_MIN      5

// ============================================================
//  ОБЪЕКТЫ
// ============================================================
Adafruit_BME280   bme;
BH1750            lightMeter;
LiquidCrystal_I2C lcd(I2C_LCD_ADDR, 16, 2);

String API_DATA_URL = "http://" + String(SERVER_HOST) + ":" + String(SERVER_PORT) + "/api/data";
String API_POMO_URL = "http://" + String(SERVER_HOST) + ":" + String(SERVER_PORT) + "/api/pomodoro";

// ============================================================
//  ДАННЫЕ ДАТЧИКОВ
// ============================================================
float sensorTemp     = NAN;
float sensorHum      = NAN;
float sensorPressure = NAN;
float sensorLight    = NAN;
float sensorNoise    = NAN;
float sensorCO2      = NAN;   // MQ-135 ppm
bool  sensorMotion   = false;

// ============================================================
//  СОСТОЯНИЕ
// ============================================================
enum AlertLevel { ALERT_OK, ALERT_WARN, ALERT_DANGER };
AlertLevel alertLevel = ALERT_OK;
String alertMessage   = "";
bool wifiConnected    = false;

enum PomoMode { POMO_IDLE, POMO_WORK, POMO_BREAK };
PomoMode pomoMode        = POMO_IDLE;
uint32_t pomoStartTime   = 0;
int      pomoSecondsLeft = 0;
int      pomoCycles      = 0;

uint8_t displayPage         = 0;
const uint8_t DISPLAY_PAGES = 5;

uint32_t lastSensorRead    = 0;
uint32_t lastDisplayChange = 0;
uint32_t lastWifiCheck     = 0;

// ============================================================
//  ISR
// ============================================================
volatile bool isrBtnMode = false;
volatile bool isrBtnOK   = false;
volatile bool isrPIR     = false;

void IRAM_ATTR onBtnMode() { isrBtnMode = true; }
void IRAM_ATTR onBtnOK()   { isrBtnOK   = true; }
void IRAM_ATTR onPIR()     { isrPIR     = true; }

// ============================================================
//  RGB LED
// ============================================================
void setLED(uint8_t r, uint8_t g, uint8_t b) {
  analogWrite(PIN_LED_R, r);
  analogWrite(PIN_LED_G, g);
  analogWrite(PIN_LED_B, b);
}

void updateLED() {
  if (!wifiConnected) {
    bool blink = (millis() / 500) % 2;
    setLED(0, 0, blink ? 200 : 0);
    return;
  }
  if (pomoMode == POMO_BREAK) { setLED(0, 0, 100);  return; }
  if (pomoMode == POMO_WORK)  { setLED(0, 150, 50); return; }
  switch (alertLevel) {
    case ALERT_OK:     setLED(0, 200, 0);   break;
    case ALERT_WARN:   setLED(200, 150, 0); break;
    case ALERT_DANGER: setLED(255, 0, 0);   break;
  }
}

// ============================================================
//  ЗУММЕР
// ============================================================
void beep(int freq, int ms) { tone(PIN_BUZZER, freq, ms); }
void beepOK()     { beep(1000, 100); }
void beepWarn()   { for (int i = 0; i < 3; i++) { beep(800, 150); delay(200); } }
void beepDanger() { for (int i = 0; i < 5; i++) { beep(500, 100); delay(150); } }
void beepPomoDone() {
  int melody[] = {1047, 1175, 1319, 1397};
  for (int n : melody) { beep(n, 200); delay(220); }
}

// ============================================================
//  LCD
// ============================================================
void lcdPrint(const char *line1, const char *line2) {
  lcd.clear();
  lcd.setCursor(0, 0); lcd.print(line1);
  lcd.setCursor(0, 1); lcd.print(line2);
}

void lcdRow(uint8_t row, const char *fmt, ...) {
  char buf[17] = {};
  va_list args;
  va_start(args, fmt);
  vsnprintf(buf, 17, fmt, args);
  va_end(args);
  for (int i = strlen(buf); i < 16; i++) buf[i] = ' ';
  lcd.setCursor(0, row);
  lcd.print(buf);
}

void updateDisplay() {
  switch (displayPage) {
    case 0:  // Температура + Влажность (BME280)
      if (!isnan(sensorTemp)) {
        lcdRow(0, "Temp:  %5.1f C  ", sensorTemp);
        lcdRow(1, "Hum:   %5.1f %%  ", sensorHum);
      } else {
        lcdPrint("BME280 ERROR    ", "Check I2C 0x76  ");
      }
      break;

    case 1:  // Давление + Освещённость
      if (!isnan(sensorPressure))
        lcdRow(0, "Press:%6.1f hPa", sensorPressure);
      else
        lcdRow(0, "Pressure: N/A   ");
      if (!isnan(sensorLight))
        lcdRow(1, "Light:%6.0f lux", sensorLight);
      else
        lcdRow(1, "Light:  N/A     ");
      break;

    case 2:  // Шум + Движение
      if (!isnan(sensorNoise))
        lcdRow(0, "Noise: %5.1f dB ", sensorNoise);
      else
        lcdRow(0, "Noise:  N/A     ");
      lcdRow(1, "Motion: %s", sensorMotion ? "YES !" : "No   ");
      break;

    case 3:  // CO2 (MQ-135) + WiFi
      if (!isnan(sensorCO2))
        lcdRow(0, "CO2:%7.0f ppm ", sensorCO2);
      else
        lcdRow(0, "CO2 MQ:   N/A   ");
      lcdRow(1, "WiFi:%s", wifiConnected ? "Connected" : "NO CONN ");
      break;

    case 4:  // Pomodoro
      if (pomoMode == POMO_IDLE) {
        lcdPrint("Pomodoro: IDLE  ", "OK - to start   ");
      } else {
        int m = pomoSecondsLeft / 60;
        int s = pomoSecondsLeft % 60;
        lcdRow(0, "Pomo [%s] #%d  ", pomoMode == POMO_WORK ? "WORK" : "REST", pomoCycles + 1);
        lcdRow(1, "Time: %02d:%02d     ", m, s);
      }
      break;
  }
}

// ============================================================
//  ЧТЕНИЕ ДАТЧИКОВ
// ============================================================
void readBME280() {
  bme.takeForcedMeasurement();
  float t = bme.readTemperature();
  float h = bme.readHumidity();
  float p = bme.readPressure() / 100.0f;
  sensorTemp     = (t < -40 || t > 85)   ? NAN : t;
  sensorHum      = (h < 0   || h > 100)  ? NAN : h;
  sensorPressure = (p < 870 || p > 1085) ? NAN : p;
}

void readBH1750() {
  float lux = lightMeter.readLightLevel();
  if (lux >= 0) sensorLight = lux;
}

void readMQ135() {
  int raw = 0;
  for (int i = 0; i < 10; i++) { raw += analogRead(PIN_MQ135); delay(5); }
  raw /= 10;
  sensorCO2 = map(raw, 0, 4095, 400, 2000);
}

void readNoise() {
  int peak = 0;
  for (int i = 0; i < 20; i++) {
    int v = analogRead(PIN_NOISE);
    if (v > peak) peak = v;
    delay(2);
  }
  sensorNoise = NOISE_DB_MIN + (peak / 4095.0f) * (NOISE_DB_MAX - NOISE_DB_MIN);
}

void readPIR() {
  if (isrPIR) { sensorMotion = true; isrPIR = false; }
  else         { sensorMotion = (digitalRead(PIN_PIR) == HIGH); }
}

// ============================================================
//  ПОРОГИ
// ============================================================
AlertLevel checkThresholds() {
  AlertLevel lvl = ALERT_OK;
  alertMessage = "";

  auto setAlert = [&](AlertLevel newLvl, const char* msg) {
    if (newLvl > lvl) { lvl = newLvl; alertMessage = msg; }
    else if (newLvl == ALERT_WARN && lvl == ALERT_WARN && alertMessage.isEmpty())
      alertMessage = msg;
  };

  if (!isnan(sensorTemp)) {
    if (sensorTemp > THRESH_TEMP_DANGER)                       setAlert(ALERT_DANGER, "TEMP TOO HIGH!");
    else if (sensorTemp > THRESH_TEMP_WARN)                    setAlert(ALERT_WARN,   "Temp elevated");
  }
  if (!isnan(sensorHum)) {
    if (sensorHum > THRESH_HUM_DANGER)                         setAlert(ALERT_DANGER, "HUMIDITY HIGH!");
    else if (sensorHum > THRESH_HUM_WARN)                      setAlert(ALERT_WARN,   "Humidity high");
  }
  if (!isnan(sensorLight) && sensorLight > 0) {
    if (sensorLight < THRESH_LIGHT_DANGER)                     setAlert(ALERT_DANGER, "LIGHT TOO LOW!");
    else if (sensorLight < THRESH_LIGHT_WARN)                  setAlert(ALERT_WARN,   "Light low");
  }
  if (!isnan(sensorNoise)) {
    if (sensorNoise > THRESH_NOISE_DANGER)                     setAlert(ALERT_DANGER, "NOISE TOO HIGH!");
    else if (sensorNoise > THRESH_NOISE_WARN)                  setAlert(ALERT_WARN,   "Noise elevated");
  }
  if (!isnan(sensorCO2)) {
    if (sensorCO2 >= THRESH_CO2_DANGER)                        setAlert(ALERT_DANGER, "CO2 DANGER!");
    else if (sensorCO2 >= THRESH_CO2_WARN)                     setAlert(ALERT_WARN,   "CO2 elevated");
  }
  return lvl;
}

// ============================================================
//  HTTP
// ============================================================
bool postSensorData() {
  if (!wifiConnected) return false;
  HTTPClient http;
  http.begin(API_DATA_URL);
  http.addHeader("Content-Type", "application/json");
  http.setTimeout(5000);

  JsonDocument doc;
  if (!isnan(sensorTemp))     doc["temperature"] = round(sensorTemp * 10) / 10.0;
  if (!isnan(sensorHum))      doc["humidity"]    = round(sensorHum * 10) / 10.0;
  if (!isnan(sensorPressure)) doc["pressure"]    = round(sensorPressure * 10) / 10.0;
  if (!isnan(sensorLight))    doc["light"]       = round(sensorLight);
  if (!isnan(sensorNoise))    doc["noise"]       = round(sensorNoise * 10) / 10.0;
  if (!isnan(sensorCO2))      doc["co2"]         = round(sensorCO2);
  doc["motion"] = sensorMotion ? 1 : 0;

  String body;
  serializeJson(doc, body);
  int code = http.POST(body);
  http.end();
  if (code == 200) { Serial.println("[HTTP] POST OK: " + body); return true; }
  Serial.printf("[HTTP] POST failed: %d\n", code);
  return false;
}

bool postPomodoroEvent(const char *type, int durationMin) {
  if (!wifiConnected) return false;
  HTTPClient http;
  http.begin(API_POMO_URL);
  http.addHeader("Content-Type", "application/json");
  http.setTimeout(5000);
  JsonDocument doc;
  doc["type"] = type; doc["duration"] = durationMin;
  String body; serializeJson(doc, body);
  int code = http.POST(body);
  http.end();
  return (code == 200);
}

// ============================================================
//  WiFi
// ============================================================
void wifiConnect() {
  Serial.printf("[WiFi] Подключение к %s ...\n", WIFI_SSID);
  lcdPrint("Connecting WiFi ", WIFI_SSID);
  setLED(0, 0, 200);
  WiFi.mode(WIFI_STA);
  WiFi.begin(WIFI_SSID, WIFI_PASSWORD);
  uint32_t t = millis();
  while (WiFi.status() != WL_CONNECTED && millis() - t < 15000) {
    delay(500); Serial.print(".");
  }
  Serial.println();
  if (WiFi.status() == WL_CONNECTED) {
    wifiConnected = true;
    Serial.println("[WiFi] Подключено! IP: " + WiFi.localIP().toString());
    char ipStr[17];
    WiFi.localIP().toString().toCharArray(ipStr, 17);
    lcdPrint("WiFi Connected! ", ipStr);
    setLED(0, 200, 0); beepOK(); delay(2000);
  } else {
    wifiConnected = false;
    Serial.println("[WiFi] ОШИБКА — автономный режим");
    lcdPrint("WiFi FAILED     ", "Working offline ");
    delay(2000);
  }
}

// ============================================================
//  POMODORO
// ============================================================
void pomoStart() {
  pomoMode = POMO_WORK; pomoStartTime = millis(); pomoSecondsLeft = POMO_WORK_MIN * 60;
  lcdPrint("Pomodoro START! ", "Work time! :)   ");
  beepOK(); delay(1000);
}

void pomoStop() {
  pomoMode = POMO_IDLE;
  lcdPrint("Pomodoro STOP   ", "                ");
  beepOK(); delay(800);
}

void updatePomodoro() {
  if (pomoMode == POMO_IDLE) return;
  uint32_t elapsed = (millis() - pomoStartTime) / 1000;
  if (pomoMode == POMO_WORK) {
    int left = POMO_WORK_MIN * 60 - (int)elapsed;
    pomoSecondsLeft = max(0, left);
    if (left <= 0) {
      pomoCycles++;
      postPomodoroEvent("work", POMO_WORK_MIN);
      beepPomoDone();
      lcdPrint("WORK DONE! Great", "Take a break!   ");
      delay(2000);
      pomoMode = POMO_BREAK; pomoStartTime = millis(); pomoSecondsLeft = POMO_BREAK_MIN * 60;
    }
  } else {
    int left = POMO_BREAK_MIN * 60 - (int)elapsed;
    pomoSecondsLeft = max(0, left);
    if (left <= 0) {
      postPomodoroEvent("break", POMO_BREAK_MIN);
      beepOK();
      lcdPrint("Break over!     ", "Press OK to cont");
      pomoMode = POMO_IDLE;
    }
  }
}

// ============================================================
//  КНОПКИ
// ============================================================
uint32_t btnModeLast = 0, btnOKLast = 0;
const uint32_t DEBOUNCE_MS = 200;

void handleButtons() {
  uint32_t now = millis();
  if (isrBtnMode && now - btnModeLast > DEBOUNCE_MS) {
    isrBtnMode = false; btnModeLast = now;
    displayPage = (displayPage + 1) % DISPLAY_PAGES;
    updateDisplay(); beepOK();
  }
  if (isrBtnOK && now - btnOKLast > DEBOUNCE_MS) {
    isrBtnOK = false; btnOKLast = now;
    if (pomoMode == POMO_IDLE) pomoStart(); else pomoStop();
  }
}

// ============================================================
//  SETUP
// ============================================================
void setup() {
  Serial.begin(115200);
  Serial.println("\n=== NEXIS v04: BME280 + MQ-135 ===");

  pinMode(PIN_PIR,      INPUT);
  pinMode(PIN_BTN_MODE, INPUT_PULLUP);
  pinMode(PIN_BTN_OK,   INPUT_PULLUP);
  pinMode(PIN_LED_R,    OUTPUT);
  pinMode(PIN_LED_G,    OUTPUT);
  pinMode(PIN_LED_B,    OUTPUT);
  pinMode(PIN_BUZZER,   OUTPUT);

  attachInterrupt(digitalPinToInterrupt(PIN_BTN_MODE), onBtnMode, FALLING);
  attachInterrupt(digitalPinToInterrupt(PIN_BTN_OK),   onBtnOK,   FALLING);
  attachInterrupt(digitalPinToInterrupt(PIN_PIR),      onPIR,     RISING);

  analogReadResolution(12);
  analogSetAttenuation(ADC_11db);
  setLED(0, 0, 50);

  Wire.begin(I2C_SDA, I2C_SCL);
  lcd.init(); lcd.backlight();
  lcdPrint("NEXIS v04       ", "Initializing... ");
  delay(500);

  if (!bme.begin(I2C_BME280_ADDR, &Wire)) {
    Serial.println("[ERR] BME280 не найден!");
    lcdPrint("BME280 ERROR!   ", "Check I2C 0x76  "); delay(2000);
  } else {
    bme.setSampling(Adafruit_BME280::MODE_FORCED,
                    Adafruit_BME280::SAMPLING_X1, Adafruit_BME280::SAMPLING_X1,
                    Adafruit_BME280::SAMPLING_X1, Adafruit_BME280::FILTER_OFF);
    Serial.println("[OK] BME280");
    lcdPrint("BME280  OK      ", "                "); delay(500);
  }

  if (lightMeter.begin(BH1750::CONTINUOUS_HIGH_RES_MODE, I2C_BH1750_ADDR)) {
    Serial.println("[OK] BH1750");
    lcdPrint("BH1750  OK      ", "                "); delay(500);
  } else {
    Serial.println("[ERR] BH1750 не найден!");
    lcdPrint("BH1750 ERROR!   ", "Check I2C 0x23  "); delay(2000);
  }

  Serial.println("[OK] MQ-135 (GPIO36 ADC1)");
  lcdPrint("MQ-135  OK      ", "ADC GPIO36      "); delay(500);

  wifiConnect();

  readBME280(); readBH1750(); readMQ135(); readNoise(); readPIR();
  alertLevel = checkThresholds();
  updateLED(); updateDisplay();

  Serial.println("─────────────────────────────");
  Serial.printf("BME280: T=%.1f°C  H=%.1f%%  P=%.1f hPa\n", sensorTemp, sensorHum, sensorPressure);
  Serial.printf("BH1750: %.0f lux\n", sensorLight);
  Serial.printf("MQ-135: %.0f ppm CO2\n", sensorCO2);
  Serial.printf("KY-037: %.1f dB\n", sensorNoise);
  Serial.printf("PIR:    %s\n", sensorMotion ? "MOTION" : "clear");
  Serial.println("[OK] Setup complete!");
}

// ============================================================
//  LOOP
// ============================================================
void loop() {
  uint32_t now = millis();

  if (now - lastSensorRead >= SENSOR_INTERVAL_MS) {
    lastSensorRead = now;
    readBME280(); readBH1750(); readMQ135(); readNoise(); readPIR();

    AlertLevel prev = alertLevel;
    alertLevel = checkThresholds();
    if (alertLevel == ALERT_DANGER && prev != ALERT_DANGER) beepDanger();
    else if (alertLevel == ALERT_WARN && prev == ALERT_OK)  beepWarn();
    updateLED();

    Serial.println("─────────────────────────────");
    Serial.printf("T=%.1f°C  H=%.1f%%  P=%.1f hPa\n", sensorTemp, sensorHum, sensorPressure);
    Serial.printf("Light=%.0f lux  Noise=%.1f dB  CO2=%.0f ppm\n",
                  sensorLight, sensorNoise, sensorCO2);
    Serial.printf("PIR: %s\n", sensorMotion ? "MOTION" : "clear");
    if (alertLevel != ALERT_OK)
      Serial.println("ALERT [" + String(alertLevel == ALERT_DANGER ? "DANGER" : "WARN") + "]: " + alertMessage);
    Serial.println("POST: " + String(postSensorData() ? "OK" : "FAILED"));
  }

  if (now - lastDisplayChange >= DISPLAY_INTERVAL_MS) {
    lastDisplayChange = now;
    displayPage = (displayPage + 1) % DISPLAY_PAGES;
    updateDisplay();
  }

  updatePomodoro();
  handleButtons();

  if (now - lastWifiCheck >= WIFI_RECONNECT_MS) {
    lastWifiCheck = now;
    if (WiFi.status() != WL_CONNECTED) {
      wifiConnected = false; WiFi.reconnect();
      uint32_t t = millis();
      while (WiFi.status() != WL_CONNECTED && millis() - t < 8000) delay(500);
      wifiConnected = (WiFi.status() == WL_CONNECTED);
      if (wifiConnected) Serial.println("[WiFi] Reconnected: " + WiFi.localIP().toString());
    } else { wifiConnected = true; }
  }

  delay(10);
}
```

---

**LCD экраны (5 страниц):**
| Страница | Строка 1 | Строка 2 |
|---------|---------|---------|
| 0 | Temp: XX.X C | Hum: XX.X % |
| 1 | Press: XXXX hPa | Light: XXXX lux |
| 2 | Noise: XX.X dB | Motion: YES!/No |
| 3 | CO2: XXXX ppm | WiFi: Connected |
| 4 | Pomo [WORK] #N | Time: MM:SS |
